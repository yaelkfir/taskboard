
const classes = ['one', 'two'];

function createClasses(classesArr) {

  let classesStr = '';

  classesArr.forEach((e) => { classesStr += ' '+ e;});

  return arrConected

}

createClasses(classes);
console.info(createClasses(classes));
